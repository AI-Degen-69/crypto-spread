# TODO — Issue #224

- [ ] T1 — live engine: `_window_clock` helper; no slug parsing; untraded when no clock
- [ ] T2 — live engine: stop exit resolves via `_resolve_exit_bid`, holds on total failure
- [ ] T3 — live engine: no `0.50` re-entry mid; no mid means no re-entry
- [ ] T4 — backtest: elapsed and window length from timestamps only; `no_clock` outcome
- [ ] T5 — tests for each fabrication removed
